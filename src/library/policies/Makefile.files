poli_OBJS = \
    policy.o \
    pc_suport.o

poli_BINS = \
    min_time.so  \
    min_energy.so \
    min_time_no_models.so \
    min_energy_no_models.so \
    monitoring.so

ifeq ($(FEAT_MPI_API), 1)
poli_BINS += \
    min_time_mpi_info.so \
    monitoring_ext.so \
    monitoring_ext_notime.so
endif

ifdef CUDA_BASE
poli_BINS += \
    gpu_monitoring.so \
    gpu_min_time.so \
    gpu_min_energy.so
endif

eard_BINS = \
    min_time_eard.so \
    min_energy_eard.so \
    monitoring_eard.so

